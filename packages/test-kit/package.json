{
  "name": "@where-they-are/test-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:smoke": "tsx scripts/smoke.ts",
    "test:coolify": "tsx scripts/coolify-smoke.ts",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@where-they-are/contracts": "workspace:*",
    "@where-they-are/server-client": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@where-they-are/config": "workspace:*",
    "tsx": "^4.20.6",
    "typescript": "catalog:",
    "vitest": "^5.0.1"
  }
}
