{
  "name": "@where-they-are/site-origin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@where-they-are/config": "workspace:*",
    "@where-they-are/server-client": "workspace:*",
    "tsx": "^4.20.6",
    "typescript": "catalog:"
  }
}
