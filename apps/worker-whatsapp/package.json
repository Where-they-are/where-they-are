{
  "name": "@where-they-are/worker-whatsapp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx --import varlock/auto-load --watch src/index.ts",
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "env:generate": "varlock codegen",
    "start": "node --import varlock/auto-load dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@mastra/core": "^1.67.0",
    "@where-they-are/server-client": "workspace:*",
    "varlock": "catalog:",
    "whatsapp-web.js": "^1.34.7",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@where-they-are/config": "workspace:*",
    "tsx": "^4.20.6",
    "typescript": "catalog:",
    "vitest": "^5.0.1"
  }
}
