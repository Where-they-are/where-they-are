{
  "name": "@where-they-are/marketing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build",
    "check-types": "tsc --noEmit",
    "dev": "astro dev --port 3000",
    "start": "astro preview --port 3000"
  },
  "dependencies": {
    "astro": "^5.15.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@where-they-are/config": "workspace:*",
    "typescript": "catalog:"
  }
}
